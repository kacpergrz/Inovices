# Finance transport module
1. Rotate the exposed service-role key.
2. Run the SQL migration.
3. Put a new publishable/anon key in index.html and finance-module.js.
4. Copy files into the repository root and supabase/migrations/.
